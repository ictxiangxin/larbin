language:
    - cpp
    - m4

os:
    - linux

before_install:
    - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
    - sudo apt-get install -qq g++-4.8
    - sudo apt-get install lynx m4 xutils-dev

install:
    ./configure
    make
